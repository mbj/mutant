source 'https://rubygems.org'

gemspec name: 'mutant'

eval_gemfile File.expand_path('../Gemfile.shared', __FILE__)

# Temporary dependency on master as preview of 0.3.5 release
gem 'regexp_parser', git: 'https://github.com/backus/regexp_parser', branch: '0.3.5-preview'
